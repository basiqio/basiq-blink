{
  "name": "basiq-blink",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "pre-commit": "lint",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^4.16.0",
    "pre-commit": "^1.2.2"
  },
  "scripts": {
    "start": "sudo nodemon index.js",
    "deploy": "aws s3 sync static s3://basiq-blink --acl public-read",
    "lint": "./node_modules/.bin/eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Basiq",
  "license": "ISC"
}
